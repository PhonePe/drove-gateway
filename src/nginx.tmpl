# Generated by nixy {{datetime}}

    {{- range $appid, $app := .MergeAppsByLabel "traefik.backend"}}
    {{- if eq (index $app.Labels "traefik.backend") "api"}}
    upstream {{ (index $app.Labels "traefik.backend") }} {
        {{- range $app.Tasks}}
        server {{ .Host }}:{{ index .Ports 0 }};
        {{- end}}
    }
    {{- end}}
    {{- if eq (index $app.Labels "traefik.backend") "nixy-marathon-events"}}
    upstream {{ (index $app.Labels "traefik.backend") }} {
        {{- range $app.Tasks}}
        server {{ .Host }}:{{ index .Ports 0 }};
        {{- end}}
    }
    {{- end}}
    {{- end}}
